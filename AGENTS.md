# AGENTS

## Package management

- Use `bun` by default for installing, removing, and running package manager commands in this repository.
- Do not use `npm install` unless the user explicitly asks for npm.
